/* Component 	: {{pascalCase ComponentName}}Container Component
 * Created on	: {{date}}
 * Created by	: {{upperCase ComponentAuthor}}
 */

/* Import Statement Below */
import React, { useState, useEffect } from 'react';
import PropTypes from 'prop-types';
import './{{pascalCase ComponentName}}.scss';

/* Type Checking for PropTypes */
const propTypes = {
  // id: PropTypes.number.isRequired,
  // name: PropTypes.string,
};

/* Declare default Props value for all non-required props */
const defaultProps = {
  // name: 'Vaibhav Shringarpure',
};

/* Functional Component Function below with state and Life Cycle Methods */
const {{pascalCase ComponentName}}Container = props => {
  const [stateItem, setStateItem] = useState([]);

  useEffect(() => {
    // LifeCycle Hook - Write Here the Methods to run using LifeCycle
  }, []);
  
  //Define your Methods Here

  //Returns JSX below
  return <div />;
};

{{pascalCase ComponentName}}Container.propTypes = propTypes; // from Line 11
{{pascalCase ComponentName}}Container.defaultProps = defaultProps; // from Line 17

export default {{pascalCase ComponentName}}Container;
