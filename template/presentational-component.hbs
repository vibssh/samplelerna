/* Component  : {{pascalCase ComponentName}} Component
 * Created on : {{date}}
 * Created by : {{upperCase ComponentAuthor}}
 */

/* Import Statement Below */
import React from 'react';
import PropTypes from 'prop-types';
import './{{pascalCase ComponentName}}.scss';

/* Type Checking for PropTypes */
const propTypes = {
  // id: PropTypes.number.isRequired,
  // name: PropTypes.string,
};

/* Declare default Props value for all non-required props */
const defaultProps = {
  // name: 'Vaibhav Shringarpure',
};

/* Functional Component Function below */
const {{pascalCase ComponentName}} = props => {
  //Define your Methods Here

  //Returns JSX below
  return <div />;
};

{{pascalCase ComponentName}}.propTypes = propTypes; // from Line 11
{{pascalCase ComponentName}}.defaultProps = defaultProps; // from Line 17

export default {{pascalCase ComponentName}};
